// #### Challenge 1: Flavor Selection
let userPreference = "Sugar-free";
console.log(
  `Challenge 1 - Preferred flavor: ${
    userPreference === "Sugar-free" ? "Sugar-free" : "Regular"
  }`
);

// #### Challenge 2: Stock Check
let cansLeft = 3;
console.log(
  `Challenge 2 - ${cansLeft < 5 ? "Time to restock!" : "We're stocked!"}`
);

// #### Challenge 3: Workout Intensity
let heartRate = 95;
console.log(
  `Challenge 3 - ${
    heartRate < 100 ? "Boost needed!" : "Energy levels are high!"
  }`
);

// #### Challenge 4: Temperature Suitability
let currentTemp = 4;
console.log(
  `Challenge 4 - ${
    currentTemp <= 5 ? "Chilled to perfection!" : "Needs a cooler!"
  }`
);

// #### Challenge 5: Late Night Coding Session
let currentHour = 22;
console.log(
  `Challenge 5 - ${
    currentHour < 24 ? "Unleash the beast!" : "Better stick to water."
  }`
);
